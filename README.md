# mddocx

`mddocx` is a deterministic Markdown → Microsoft Word compiler. Version **1.2.0** adds privacy-aware AI/chat export metadata sanitization while preserving the stable v1 compiler/API contract.

See the full project documentation and source tree in the upcoming upload.